id: "FIXTURE-CANDIDATE-HARNESS-001"
version: v1
title: 0330 乘客端小程序 PRD 通用候选用例测试数据
description: "由 candidate-to-harness 生成的最小执行数据模板，正式执行前需按模块细化。"
data_scope: synthetic
isolation:
  strategy: unique_ids
  collision_policy: fail_fast
environment:
  target: test
  network_profile: normal
entities:
  - id: passenger_candidate_001
    type: passenger
    initial_state: registered
  - id: order_candidate_001
    type: order
    initial_state: available_when_needed
secrets:
  required: false
  source: external_secret_manager
cleanup:
  required: true
  actions:
    - purge_synthetic_records
    - reset_mock_state
safety:
  production_safe: false
  pii_allowed: false
  notes: 仅允许合成数据。
